CREATE TABLE customers (id      INTEGER PRIMARY KEY,
				   name        VARCHAR(50),
                   address        VARCHAR(100),
				   city 		VARCHAR(50),
                   age      INTEGER,
                   accnum     DOUBLE,
                   timeEnter DATE);