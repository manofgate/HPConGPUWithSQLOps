CREATE TABLE items (id      INTEGER PRIMARY KEY,
				   name        VARCHAR(50),
                   description        VARCHAR(255),
				   sold INTEGER,
                   price     DOUBLE,
                   timeEnter DATE);